package org.swampscottcurrents.serpentframework;

import edu.wpi.first.wpilibj2.command.*;

/** Represents a subsystem with additional, customizable functionality, like an automatic reset method called after the command using the subsystem exits. */
public class SerpentSubsystem extends SubsystemBase {
    /** Resets the subsystem, returning all components to their default/neutral states. Called automatically by SerpentCommands. */
    public void reset() {}
}